# =============================================================================
# Enthropy - Kubernetes Namespace
# =============================================================================
apiVersion: v1
kind: Namespace
metadata:
  name: enthropy
  labels:
    app.kubernetes.io/name: enthropy
    app.kubernetes.io/part-of: enthropy-platform
    app.kubernetes.io/managed-by: kubectl
  annotations:
    description: "Enthropy Quantitative Trading Platform"

---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: enthropy-quota
  namespace: enthropy
spec:
  hard:
    requests.cpu: "20"
    requests.memory: 40Gi
    limits.cpu: "40"
    limits.memory: 80Gi
    pods: "100"
    services: "20"
    persistentvolumeclaims: "30"

---
apiVersion: v1
kind: LimitRange
metadata:
  name: enthropy-limit-range
  namespace: enthropy
spec:
  limits:
    - type: Container
      default:
        cpu: 500m
        memory: 512Mi
      defaultRequest:
        cpu: 100m
        memory: 128Mi
      max:
        cpu: "4"
        memory: 8Gi
      min:
        cpu: 50m
        memory: 64Mi

---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: enthropy-default-deny
  namespace: enthropy
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              app.kubernetes.io/name: enthropy
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              app.kubernetes.io/name: enthropy
    - to: []
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
        - port: 443
          protocol: TCP
